# Log — 2026-08-21

## 17:21 — Downloaded FlashMemory-Deepseek-V4 from ModelScope

- **What:** Fetched `hf/libertywing-FlashMemory-Deepseek-V4` (no HF copy exists; not on disk anywhere beforehand). Model is the FlashMemory KV-cache compression kit for DeepSeek-V4: single ~486 MiB adapter weights + retriever/demo scripts.
- **How:** `apt-get install -y git-lfs` (was missing), then `git clone https://www.modelscope.cn/hf/libertywing-FlashMemory-Deepseek-V4.git` into `model_cache/FlashMemory-Deepseek-V4/`.
- **Result:** `weights/flashmemory_ds_v4.safetensors` = 509,633,992 B, sha256 `ba20d264c309246f824d4471ccc637061b3b0268fe8e4eecc121474a1e5cd02a` (matches API `X-Linked-Etag`); `git lfs ls-files` shows `*` (real LFS object, not pointer). Plus `configuration.json`, `demo.py`, `retriever.py`, `toy_flashmemory_inference.py`, `README.md`, `LICENSE`, `requirements.txt`.
- **Blockers / fixes:** git-lfs not installed → installed via apt; full-disk `find` for the model was still running when download finished → killed (bg task).
- **Next:** inspect README/demo to see how the FlashMemory retriever plugs into DeepSeek-V4 inference, if a run is planned.
