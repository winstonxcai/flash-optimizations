# Log — 2026-09-05

## 22:29 — Sangfor native baseline-rerun COMPLETE (22/50): report native column flipped to controlled run; result now +2 Packed, net-zero across agentic suites

- **Native untouched-0731 TP4 baseline-rerun done** (`mustafar-dsv4-0731-sangforbench50-baseline-rerun`, eval box 10.57.3.76, our :30212 native server, Claude Code 2.1.258 / max_workers 8 — identical harness config to the packed leg). Phase-1 (agent fix generation) 18:36→21:08 (~2h30m, 50/50 patches, 0 errors; the e6be6a64 straggler ran 1h17m — it had the *only* phase-1 container left up at 21:07). Phase-2 (repo-test eval) 21:09→22:16 (`cli.py:320 Done.`, 50/50 test_result.json; the single 22:15 `[ERROR] external.py:48 ai-code-eval 源码目录不存在` is a benign teardown warning about the unused ai-code-eval benchmark mode, not our Sangfor results). **Final: native 22/50 passes (pass_rate==100).**
- **RESULT FLIPS THE SIGN vs the ACG112 reference.** Controlled native (22) vs packed (24) = **+2 Packed**, not −4. Confusion (rows=Native, cols=Packed) on the same 50: **both-pass 20 / native-only 2 / packed-only 4 / both-fail 24**. Native-only: bd1480f8 (packed 82.76), 09299ad2 (packed 29.17). Packed-only: 6720ec16 (native build_error/pr=0), ef78d2c0 (native 70.0), 9207ca23 (native 91.3), 16cce01 (native 66.67). Only 6 discordant pairs → McNemar brackets zero. Native's 22 vs ACG112's 28 on the same set is harness-generation/run-to-run spread (ACG112: Claude Code 2.1.220, host 10.57.3.35:30101, max_workers 10) — validating the user's call to replace the external reference with our controlled run.
- **report.md updated + pushed (28f4275):** Sangfor row now Native 22/50 → Packed 24/50 (**+2**); SWE row unchanged 32/50→30/50 (−2); headline "net-neutral, 54/100 vs 54/100 combined"; Sangfor confusion matrix + prose rewritten; footnote explains the ACG112→controlled replacement (incl. the 28→22 drop as harness spread); conclusion updated to "+2/−2 within noise". Git: flash-optimizations origin/main 28f4275. Poll cron 0196fd5f deleted.
- **4 build_error instances in native leg** (6720ec16, 78244fbb, sri_ccs-monitor_05b22ef3 pr=98.41, e6be6a64 pr=94.12) — all have pass_rate<100, counted as fail under the uniform ==100 criterion; same handling as packed.
- **Next:** native :30212 server still up (unused now — phase-2 was model-free); can `./serve.sh native stop`. Accuracy story is final: Sangfor +2 / SWE −2 (net-zero) on our own controlled legs.

## 17:57 — report.md accuracy rows finalized on 0731 + pushed; LB2 deferred; packed-server log check clean for accuracy legs

- **User decision this session: "Defer LB2 for now."** Accuracy story = Sangfor + SWE + LSWB; drop the LongBench v2 full runs (both legs). The LB2-packed full attempt had already OOM-crashed (see below) and the old n=100 rows used a predecessor mechanism, so they're out.
- **report.md benchmark section rewritten + pushed (origin/main 48d4663):** removed RULER 64k and LongBench v2 rows; table is now Sangfor-Bench 28/50→24/50 (−4 tasks) and SWE-bench 32/50→30/50 (−2). Sangfor Native = **ACG112 `bash_ds_flash` untouched-0731 reference** (same 50-instance hard set, pass criterion `pass_rate==100`, reproduces its known 28); packed = our mustafar packed-328 0731 (24). Confusion matrix (rows=Native, cols=Packed): both-pass 22 / native-only 6 / packed-only 2 / both-fail 20. SWE rows validated against results.json on the eval box (native `deepseekv4-flash-0731-swebench` resolved 32; packed `mustafar-dsv4-0731-swebench50` resolved 30), kept at TP8 per footnote. Conclusion softened to "within a few tasks on the two 50-task agentic evals."
- **Second commit 8667dd1** = scripts/local refactor (53 scripts → archive/, clean drivers). Working tree clean except untracked `mustafar/results/` (158 MB, deliberately not committed).
- **Polluted LB2 run discarded** (`results/lb2-full/packed/20260905_171442/`, 438 recs / 432 conn errors).
- **Packed-server error-log check (user asked):** the TP4 packed 328-B accuracy server (`serve_sangfor_packed_0731.log`, :30212, GPUs 4-7, pool 4,373,760) is CLEAN — 0 CUDA-OOM/exceptions over 12:53→16:57 local; graceful kill by watcher. The crash lives in the *next* boot (`serve_packed.log`): LB2-full OOM at 17:16 local — `Tried to allocate 4.78 GiB, 4.74 GiB free, 74.43 GiB in use` (GPU idx 1/2 = phys 5/6), mem-frac-0.88 headroom exhausted by a full-context DSV4-indexer prefill workspace alloc. Both logs are TP4 (ranks TP0–TP3; the `--tp 8` grep hits are text inside DeepGEMM JIT hints, not argv). Currently no server running (30211/30212 down, GPUs 4-7 idle).
- **TP4-vs-TP8 question (user asked):** answered — TP is serving geometry, not a model change; expected no accuracy effect for greedy agentic evals (identical per-head attention; only fp reduction order differs). Report mixes TP4 (Sangfor) and TP8 (SWE) rows; acceptable. Optional airtight check = re-serve packed 0731 at TP8 and re-run the 8 Sangfor disagreement instances.
- **Security:** stray `mustafar/scripts/config.json` contains a LIVE `ANTHROPIC_AUTH_TOKEN` (sk-1f83…); gitignored it (never commit). Not pushed. User should rotate or relocate it.
- **Next:** optional TP8 probe of the 8 Sangfor disagreements; nothing else pending until the user reopens LB2.

- **Refactor of `flash-optimizations/mustafar/scripts/local/` done + verified.** 53 one-off `.sh` moved to `archive/` (launch_inner_*, 262k runners, watchdogs, sweep drivers, tp8 launchers). Clean set = `env.sh` (shared config + helpers, MACHINE CONFIG block for porting) + 9 minimal drivers: `serve.sh native|packed [stop]`, `bench-fair.sh`/`bench-max.sh` (→ `bench-serving.sh`), `bench-lswb.sh`, `eval-lb2.sh`, `eval-sangfor.sh`/`eval-swe.sh` (→ `agentic-eval.sh`) + `lb2_serve_eval.py` + README. All 10 `.sh` pass `bash -n`; none reference an archived file (the one grep hit was the word "rerun" in a comment). `env.sh` run defaults now `${VAR:-default}` so GPUS/PORT/MEM_FRAC/… are overridable per-node.
- **Sangfor-0731 PACKED phase 1 COMPLETE**: 50 started / 50 finished / **50 patches saved**, 0 errors (as of ~15:24). Phase 2 (test eval, no model calls) now running on the eval box — `test_result.json` per instance streaming into `results/mustafar-dsv4-0731-sangforbench50/.../<instance>/`. 30212 server still up (healthy, `/v1/models` OK) but only needed for phase 1, so it's free to kill.
- **LB2-packed handoff**: watcher agent a0b801c7 (polling every ~5 min) will, on seeing 50/50: kill the 30212 server → `serve.sh packed` (log `logs/serve_packed.log`, must show `logical_row_bytes=328 layers=21`) → run `eval-lb2.sh` full into `results/lb2-full/packed/<ts>/`. It read `env.sh`/`serve.sh` defaults (GPUS 4-7, port 30212) to align with the canonical drivers.
- **Lesson (recorded):** never use `ct` (docker exec) to check a command string — it executes it. Earlier accidental tp1 launch (mem 0.841, no parsers) OOM'd against the live Sangfor server; its crash log now sits in `serve_packed.log` (mtime 15:22:33, will be truncated on the real boot). The stray's `server_args` dump did confirm serve.sh's `--cuda-graph-config '$DECODE_CFG'` quoting parses correctly.
- **Next:** LB2 packed (watcher) → boot native untouched 0731 (`serve.sh native`, GPUs 4-7) + LB2 native full → replace report rows (Sangfor packed-0731 vs ACG112 native; LB2 full both legs; drop RULER row) → commit + push origin/main.

## 14:44 — LB2-full server client built + smoke-tested; Sangfor packed at 27/50; native LB2 launcher ready; watcher agent launched

- **LB2 v2 full = 503 questions; 30 exceed the 1M ctx cap** (longest 4.64M tok) → dropped; **473 feasible, 65.2M prompt tokens**. User-facing scope: run the 473; truncation would bias (answer can sit anywhere in a >1M context). At measured fp4-native prefill ~11.8–13.8k tok/s: pure prefill ~87 min/leg → realistic **~1.5–2.25 h per leg**; both legs sequential on GPUs 4-7 ≈ 3–4.5 h.
- **`scripts/local/lb2_serve_eval.py`** — new server-mode LB2 client (threaded, concurrency 6, incremental --out resume). Mirrors `sg_capture.py::cmd_run_lb2` prompt + `_lb2_extract` exactly, but delivers as role=user chat-completions so the server chat template supplies the `<｜User｜>/<｜Assistant｜>` markers; deepseek-v4 reasoning parser handles think. max_tokens 512 (= cmd_run_lb2 --max-new), temperature 0, 1 retry. Unit-tested extraction; **smoke-tested live** on the packed 30212 server (shortest sample, 10k ctx): HTTP 200, model deepseek-v4-flash, 33 completion tokens, no reasoning_tokens emitted (direct answer), extraction clean.
- **`launch_inner_lb2_native_0731_tp4.sh`** — native-untouched LB2 launcher = Sangfor packed launcher with only TopMag envs removed (TOPMAG off, dense 584-B, sglang-only PYTHONPATH, fp4-native fixed, same parsers + decode graphs bs[1..15]/15 + expandable_segments, GPUs 4-7 / 30212). **`launch_inner_lb2_packed_0731_tp4.sh`** — same as Sangfor packed launcher, clean `serve_lb2_packed_0731.log`. diff confirms native differs only in TopMag/PYTHONPATH.
- **Sangfor-0731 PACKED (mustafar-dsv4-0731-sangforbench50) phase 1** @ 14:38: started 27, finished 20 of 50, 73 non-empty patches, 8 workers up. Pace ~2–3/12 min → phase-1 done est. ~16:30–17:00; server frees at phase-1 end (phase 2 = tests, no model calls).
- **Watcher agent (a0b801c7) launched** to poll the remote run every ~10 min and, when phase-1 completes: kill 30212 server → fresh-boot packed (verify `logical_row_bytes=328`) → launch LB2-full packed leg → report accuracy. (CronCreate was temporarily unavailable — classifier timeout — so a background agent drives the wait.)
- **Next:** LB2 packed (watcher), then native 0731 boot + LB2 native; replace report rows (Sangfor 28/50-native vs packed; LB2 full; drop RULER), commit + push.


## 12:59 — Stock-native serving sweep COMPLETE + report.md Native column re-baselined to TRUE stock; Sangfor packed server up

- **Stock sweep done** (`results/serve-sweep-fp4/stock/20260905_104540/`, all 4 points OK): Native re-measured as genuinely untouched 0731 (TOPMAG=0, dense 584-B) at its allocator ceiling: 32k C107 **11,840 tok/s**, 64k C55 **12,251**, 128k C28 **13,834**, 256k C14 **12,490**. Stock is ~1.0–1.4% faster than the old pruned-native (TOPMAG=1/PACKED_C4=0) column at every context (11,840 vs 11,697; 12,251 vs 12,076; 13,834 vs 13,693; 12,490 vs 12,369) — pruning-without-packing carries a small real decode-side cost.
- **Re-baselined deltas vs TRUE stock** — the report's claim weakens slightly: fair (packed @ native C) **−3.2% / +0.2% / −2.0% / −1.4%**; max (packed @ own C 129/66/33/17) **+0.0% / +3.4% / −0.7% / −0.5%**. TTFT at max rose 18–37%, TPOT up to 24%. Summary + Fair/Max tables + both prose blocks updated in report.md to stock numbers (Scope/config already described Native as stock 584-B, now actually true).
- **report.md committed + pushed to origin/main** (report.md only).
- **Sangfor-0731 PACKED server booted** on GPUs 4-7 / port 30212 via `launch_inner_sangfor_packed_0731_tp4.sh`: pool 4,373,760 (expandable_segments), packed guard confirmed (`logical_row_bytes=328 layers=21` ×4), parsers live (reasoning deepseek-v4, tool deepseekv4), fp4-native mxfp4, decode graphs bs[1..15] max_bs 15, smoke round-trip OK ("pong"). Log `logs/serve_sangfor_packed_0731.log`. Ready for the hard50 eval client.
- **Next:** run the Sangfor hard50 packed eval (YJYBench harness on 10.57.3.76) against :30212; delete cron 3a652036 (stock sweep validated).


## 10:33 — Baseline decision: random-serving "Native" re-run as TRUE stock (TOPMAG=0); 384k verify says flat wall holds at shallow concurrency

- **Baseline problem (user-flagged):** the report's random-serving "Native" column actually ran with `SGLANG_OPT_TOPMAG=1` + `PACKED_C4=0` — pruned-native 584-B C4, a *packing-only* control, NOT untouched stock. The report's Scope/config labels Native as "TopMag off / stock 584-B", contradicting the launcher. User approved **"Re-run as stock (TOPMAG=0)"**, message: "i wanted the native untouched vs packed 328B" — so Native = genuinely untouched 0731 (dense 584-B) vs Packed (full Mustafar), consistent with the LSWB/SWE-bench legs. The old pruned-native numbers become an internal packing-only ablation.
- **384k shallow verify COMPLETE** (results/serve-sweep-fp4/verify384k-shallow/20260905_090721, all 3 points OK): native-pruned C9 **10,969 tok/s**, packed-fair C9 **11,040** (+0.65%), packed-max C11 **11,185** (**+1.97%**). **No reproduction of the FP8-era +14.5%** — even at 384k where every request pins ~the full pool (C9/C11), packed's extra resident slots buy ~nothing: TTFT +20% (163→196 s), TPOT +22% (77→94 ms), throughput flat. Flat/prefill-bound wall confirmed at the shallow extreme too. (Native point was pruned-native; see re-run below.)
- **Stock-native re-run launched 02:33Z** on GPUs 4-7 (detached, logs/run-serve-sweep-stock.log + logs/serve_sweep_stock.log): Phase A = 4 sweep points as stock, no expandable_segments, pool gate **3,730,944** (32k C107 / 64k C55 / 128k C28 / 256k C14); Phase B = 384k native C9 as stock, expandable_segments, pool gate **3,610,880**. Same protocol as run_serve_sweep_fp4.sh (warmup C + 3C, seed 42, flush-cache), extended decode graphs max_bs 136, port 30212/master 29628.
- **Files:** `scripts/local/launch_inner_serve_sweep_stock_tp4.sh` (sweep launcher: NO TopMag envs, PYTHONPATH = sglang python only), `scripts/local/launch_inner_verify384k_stock_tp4.sh` (same + expandable_segments), `scripts/local/run_serve_sweep_stock.sh` (driver, both phases). Pruned-native artifacts preserved under results/serve-sweep-fp4/native/20260905_053911/.
- **UPDATE 02:46Z — 384k Phase B CANCELLED per user** ("lets cancel the 384k run"); driver now sweep-only (RUN_V384=1 re-enables Phase B). Killed the first (partial) driver run ~5 min into the 32k point, cleaned the stale artifacts, relaunched clean at 02:45:44Z (pid 2351592, port 30212, boot pool gate 3,730,944 OK). Expected: 4 points OK ~04:30Z.
- **Next:** on completion assemble stock-native-vs-packed fair/max tables (native now stock; packed points unchanged) and rewrite report.md Scope/config + tables; commit + push; add 384k-shallow verdict to report only if a row is warranted.

## 08:15 — fp4 serving-sweep rows REPLACE the FP8-era tables in report.md (user approved replacement)

- **Final 12-point dataset assembled** from `results/serve-sweep-fp4/` exact artifacts (measured.jsonl + server.delta.log). Fair (packed @ native C = 107/55/28/14): **−2.0% / +1.6% / −1.0% / −0.4%** tokens/s at 32k/64k/128k/256k. Max (packed @ own C = 129/66/33/17): **+1.2% / +4.9% / +0.3% / +0.5%**, median TTFT +17–36%, median TPOT up to +22%. Concurrency gains 107→129, 55→66, 28→33, 14→17 (1.18–1.21×, matching the 1.2112× pool ratio).
- **report.md serving section rewritten** on the fp4-native stack: Summary paras (pool 3,730,944→4,519,168, the four ceilings, throughput-neutral fair / prefill-bound max finding), methodology (fork v0.5.15 @ f63458b, flashinfer_mxfp4, mem-frac 0.88, fp8 KV, decode graphs max_bs 136, warmup C + 3C measured waves), the Fair + Max tables replacing the FP8-era rows (332,288→402,688, C9/11, +14–23% max), 256k-max fresh-boot rerun footnote, and Conclusion. Grep-clean: no stale FP8-era numbers remain. The LSWB replay subsection is unchanged and now consistent (it already used fp4-native pools).
- **report.md diff = prior-session LSWB subsection (was uncommitted) + this rewrite**; committed to flash-optimizations and pushed to origin/main.


## 07:20 — fp4 serving sweep COMPLETE (packed 7/8 + native 4/4 valid); 1 packed 256k-max point OOM'd, retry running

- **Sweep finished:** orchestrator `ALL DONE rc=0` at 23:05Z (host 07:05). Packed leg: 7/8 points valid; native leg: 4/4 valid. All validated (completed==3C, all output 2048, 0 errors, decode-graph replay present in deltas). See `results/serve-sweep-fp4/{packed/20260905_025227,native/20260905_053911}/`.
- **Packed ctx262144-max FAILED (measured rc=1):** CUDA OOM killed the server mid-measurement — `Tried to allocate 1.81 GiB, 1.71 GiB free` on TP1/TP2 at 21:37:36Z, after ~2.8h server lifetime / 7 prior points. Same absolute pool occupancy (4.49M tokens) succeeded at 32k-C129 early in the run → open question: 256k-depth workspace ceiling vs accumulated memory fragmentation. Retry on a FRESH boot distinguishes them (see below).
- **Latency anomaly investigated:** server `#running-req` peaks (e.g., 214 at 32k-max C=129) exceed the bench's client `max_concurrency`; bench uses a per-request `asyncio.Semaphore(C)` around the full stream, so the client in-flight is genuinely capped — the higher server running counts are finished-but-unflushed responses at wave boundaries, NOT a clean residency signal. Will NOT over-claim residency in the writeup; rely on client-side bench metrics.
- **Report.md updated** (earlier, 03:36): added "Real-workload serving: 1200 s business-conversation replay" subsection (LSWB packed-vs-untouched, full ttft/tpot/e2e + matched-845 subset) right before `## Benchmark test results`.
- **Retry running:** `scripts/local/rerun_packed_256k_max.sh` — fresh packed boot (30211), try C=17 → fall back C=16 → C=15, first passing measured wave wins, RESULT.txt written. Log: `logs/rerun256k-run.log`, monitor cron 890f747c (every 20 min @ :13/:33/:53).
- **Blemish to note:** the retry's first (buggy) launch truncated the original 4.6MB `serve_sweep_packed.log` to 18KB before being killed (CT_LOG pointed at the wrong file); the launcher hardcodes its log path. Per-point `server.delta.log` evidence is intact in each point dir, so no material loss. Fixed script points CT_LOG at `serve_sweep_packed.log`; original preserved as `serve_sweep_packed.orig_run.log` (already-truncated).

## 03:10 — fp4-native serving-capacity sweep: packed leg running (~4h total), extended decode graphs validated

- **What:** Serving-capacity sweep of TopMag50 native-584 vs packed-328 C4 (DeepSeek-V4-Flash-0731, TP4, fp4-native LSWB stack) at 32k/64k/128k/256k, exactly 2048 output, fair + per-mode-max concurrency. This is todo-3 of the "do the serving sweep first" reordering; results become fresh rows in `mustafar/report.md` serving section (the user's 0731 fp4-native stack choice replaces the FP8 rows).
- **Design:** Native leg = TOPMAG=1 KEEP=0.5 PACKED_C4=0 (packing-only delta); packed = PACKED_C4=1. Pools: native 3,730,944 / packed 4,519,168 (1.2112×) → allocator ceilings native C107/55/28/14, packed C129/66/33/17 at 32k/64k/128k/256k. Per point: warmup 1 wave (C) + measured 3 waves (3C) via official `sglang.bench_serving` (report.md protocol), `--random-range-ratio 1.0`, seed 42, flush-cache.
- **Decode CUDA graphs:** extended LSWB bs[1..15] → sparse buckets through max_bs 136 (same config both legs) so decode stays on-graph up to C129. Validated in a boot: capture 31s/rank, **pool intact at 4,519,168** (graphs capture after pool), avail_gpu_mem 6.41 GB, health OK. Decode-graph extension confirmed safe.
- **Platform reality:** the fp4 stack prefills at only ~17.4k tok/s input (steady-state, single-8192-chunk rounds), so each point (~18M prefill tokens ≈ 11× report.md's FP8 points) takes ~20-25 min. Full 12-point 3-wave protocol ≈ 3.5-4h. User chose **full report.md waves (~4h)** over trimming.
- **Files:** `scripts/local/launch_inner_serve_sweep_fp4_tp4.sh` (LEG=native|packed inner launcher, CUDA_VISIBLE_DEVICES=4,5,6,7, ports 30212/29628 native, 30211/29626 packed), `scripts/local/run_serve_sweep_fp4.sh` (per-leg driver, pool-expectation gate 4519168/3730944, kills own server at exit), `scripts/local/orchestrate_serve_sweep_fp4.sh` (chains packed→native), `scripts/local/serve_sweep_parse.py` (assembles measured records).
- **Progress:** validation boot + packed boot passed; ctx32768-fair measured 140/321 at ~10.5 min (~2.6 s/it) → packed leg ≈ 2.5-2.8h, native ≈ 1h. Running on host GPUs 4-7 inside ruler-eval (fork v0.5.15 @ f63458b). Results to `mustafar/results/serve-sweep-fp4/<leg>/<ts>/`, logs `mustafar/logs/serve_sweep_{packed,native}.log`.
- **Next:** on completion, run `serve_sweep_parse.py` to assemble the fair + max tables (native@C_nat vs packed@C_nat fair; native@C_nat vs packed@C_pck max), verify residency (running_max ≈ C) + decode-graph replay from server.delta.log, then writeup + report.md rows. Note: if the workload is prefill-bound (17k/s) rather than capacity-bound at these depths, packed's +21% pool may show little throughput gain — a finding to report honestly.
