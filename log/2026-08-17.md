# Log — 2026-08-17

## 23:05 — Full-RULER TopMag prune sweep @64k, n=100: STRONG GO (850 samples, 2x tp=4)

- **What:** Extend the CSA compressed-latent TopMag prune go/no-go to the long-context
  regime: all 13 RULER tasks @64k, n=100 where data allows (4 tasks at 100: niah_multikey_2,
  vt, fwe, qa_2; 9 tasks at 50) → 850 samples/config, keep {0.5, 0.3}, tp=4.
- **How:** Container `sglang-44039`, two concurrent `run-acc --prune-keep {0.5,0.3}
  --lengths 64k --n-64k 100 --tp 4` on GPUs 0-3 (MASTER_PORT=29501, sg_ctrl_prune50_64k) and
  4-7 (29500, sg_ctrl_prune70_64k); `--mem-fraction 0.95` (tp=4 was the max that fit the fp32
  capture pass at 64k). Launched n=50 first, resumed to n=100 for the 4 n-100 tasks. Artifacts:
  `out/ruler_csa_prune50_64k.json`, `out/ruler_csa_prune70_64k.json` (850 samples each); logs
  `par_prune50/70_64k.log`.
- **Result:** MEAN dense 0.951 / pr50 0.953 / pr70 0.947 → d50 = **-0.16 pts**, d70 = **+0.39 pts**
  (both ≤2); R(0.5)=0.954, R(0.7)=0.845. Per-task deltas mostly ≤ 0.7 pts (prune often helps:
  niah_multikey_2 -0.50, vt -0.70). Soft spot: **qa_2 @70% sparsity +4.50 pts** (dense 0.735 →
  pr70 0.690), qa_1 @70% +2.00 (n=50). **Verdict: STRONG GO** — 64k confirms the 32k result.
- **Blockers / fixes:** (1) `--n-64k 100` runs produced 50 samples/task initially — surviving
  earlier `--n 50` procs, not a code bug (argparse + smoke test confirmed); relaunched with
  `--n-64k 100` on the same out paths to resume. (2) TaskStop doesn't kill docker-exec'd python
  children — `pkill -9 -f "sglang::scheduler"` needed to free GPUs (GPU 6 lingered at 72 GB).
  (3) Launch-time sleep-based probes hit the 120s tool cap; used short commands instead.
- **Next:** qa_2's 70%-sparsity 4.5-pt drop at 64k (largest on the board) — decide whether to
  cap effective sparsity for QA-family or ship 50% only for long-context; draft writeup for the
  32k+64k verdict (writeup skill, needs approval).

## 20:18 — CSA TopMag prune go/no-go @32k: STRONG GO (4 tasks x 50, parallel 2x tp=4)

- **What:** Ran the Mustafar→V4-Flash CSA compressed-cache TopMag prune eval to completion:
  smoke (n=3, niah_multikey_2+vt) then full 4 tasks x 50 @32k x keep{0.5,0.3}, tp=4.
- **How:** Container `sglang-44039`, two concurrent `run-acc --prune-keep {0.5,0.3}` on
  GPUs 0-3 (MASTER_PORT=29501) and 4-7 (29500), separate `--ctrl-dir` (sg_ctrl_prune50/70)
  to isolate the meta.json + retained-energy payload namespace. Artifacts:
  `out/ruler_csa_prune50.json`, `out/ruler_csa_prune70.json`; logs `par_prune50/70.log`;
  launchers `sg_prune_smoke.sh`, `sg_prune_par.sh`.
- **Result:** MEAN dense 0.933 / pr50 0.935 / pr70 0.927 → d50 = **-0.23 pts**, d70 = **+0.60 pts**
  (both ≤2); R(0.5)=0.955, R(0.7)=0.850. Per-task (pts): niah_multikey_2 0/0, vt -0.6/-0.6,
  fwe +0.67/0.00, qa_2 -1.0/+3.0 (only soft spot: qa_2 drops 3 pts at 70% sparsity, dense=0.75
  @n=50). **Verdict: STRONG GO.** prune50 always ≥ dense; dense matches recorded 32k baseline.
  Wall: prune50 16.8 min, prune70 ~19 min (parallel).
- **Blockers:** (1) parallel run needed distinct MASTER_PORT — default 29500 collided
  (EADDRINUSE) on the 2nd engine; (2) the failed 2nd engine poisoned the co-started engine's
  init (hung pre-load) — relaunched clean after kill; (3) run-acc leaves orphaned sglang
  scheduler procs holding GPUs after driver exit — killed them post-run.
- **Next:** consider qa_2's 70% sensitivity (3-pt drop) before adopting 70% sparsity; optionally
  draft a writeup for this verdict.

- **What:** Implemented the first experiment for testing whether Mustafar-style magnitude
  pruning transfers to V4-Flash's native CSA compressed cache (`C^Comp ∈ ℝ^512`, 21 CSA
  layers, Shared-KV). Conditions: Baseline / CSA-50% (keep 0.5) / CSA-70% (keep 0.3), TopMag
  per compressed vector. Measures: RULER score (dense + each config, re-measured in-run) and
  retained energy `R(s)=‖C̃‖²_F/‖C‖²_F` (aggregate + per-layer). Go rule: 50% drop ≤2 pts &
  R(0.5)>0.90; Strong-go if 70% also ≤2 pts.
- **How:** All in `transferibility/sg_capture.py`:
  - Worker (`on_compress` inject branch, pre-RoPE raw latent): per-row keep top-k by |normed|
    (RMSNorm(raw)·weight), zero the raw coords of the rest (fused store renormalizes as usual),
    accumulate per-layer Σ‖target‖²/Σ‖orig‖². Persists `sg_ctrl/results/{tag}.json`
    `retained_energy` payload at prefill→decode flush (`_analyze_and_flush`). No changes to
    HCA, indexer, or compressor_v2.py patch targets (interface unchanged).
  - Driver: `run-acc --prune-keep {0.5,0.3}` → pass1 dense + pass2 prune inject per prompt,
    polls the energy payload, writes per-sample `{dense_score, prune_score, prune_keep,
    retained_energy}` to a fresh `--out`; dense runs lighter than the W3 path (no fp32 capture).
  - New `report-prune` subcommand: per-task + mean dense/pr50/pr70, Δ points, mean R + per-layer
    band, applies the go/no-go verdict.
- **Result:** Logic verified inside `ruler-eval` container (synthetic `on_compress`): keep=0.5 →
  exactly 256/row, keep=0.3 → 153/row, kept-set == topk(|normed|), R(0.5)=0.933 / R(0.7)=0.790
  on Gaussian data; flush payload schema correct; `report-prune` table + verdict print correctly.
  Host `py_compile` OK; live compressor_v2.py still calls the unchanged
  `on_compress(kv_compressed, plan, norm, compress_ratio, is_indexer, head_dim)`.
- **Next:** Smoke `run-acc --prune-keep 0.5/0.3` n=3 @32k on `niah_multikey_2,vt` (dense ≈
  recorded 32k baseline, energy payload sane), then full 4 tasks × 96 @32k × both configs on
  GPUs 0–3 (tp=4). Then `report-prune` → verdict.
