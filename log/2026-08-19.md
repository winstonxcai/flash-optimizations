# Log — 2026-08-19

## 17:59 — §6 xKV cross-layer low-rank on CSA indexer COMPLETE → **NOT free @64k** (−1.1 pts)

- **What:** The sibling §6 run (W3@b64 cross-layer SVD on the indexer's 128-dim keys, 2:1 compression,
  `--recon-target indexer`) finished, run in parallel with the §7 TopMag legs. Verdict recorded in
  `writeup/xkv-crosslayer.md` Part 4 + `experiments.md` §6.
- **How:** Two parallel tp=4 legs on 8 GPUs (qa_2,qa_1,fwe on 0-3; vt,niah_multivalue on 4-7),
  container `ruler-eval`, launcher `transferibility/sg_idx_w3_64k_par.sh`. 64k, 5 hardest tasks × n=50.
  Out: `transferibility/out/ruler_csa_idx_w3_64k{,_a,_b}.json`.
- **Result:** MEAN 0.888 (native) → 0.877 (W3@b64), **Δ = +1.1 pts**. fwe +4.0, qa_1 +2.0; retrieval/
  needle free (niah_multivalue 0.0, vt −0.4); qa_2 0.0. Not free vs the latent's −0.004 @32k — the
  indexer is a hard top-512 *selector*, so basis error moves tokens across the selection boundary.
  Only-indexer proof (1008 compress + 504 inject events, zero d=512). 64k feasible here where §5
  deferred it (indexer capture `[T,128]`, 4× smaller than the OOM'ing `[T,512]` latent).
- **Contrast with §7 (17:30):** TopMag on the same indexer keys is **STRONG GO** (Δ70 0.55 pt); xKV
  W3@b64 low-rank is **not free** (−1.1 pts). The magnitude-prune path is the accuracy-safe indexer
  compute lever at 64k; low-rank needs a higher rank / shorter context.
- **Next:** none pending for §6/§7 — both writeups written. Open: 32k indexer TopMag resume (13
  tasks × n=100) and the 256k/tp=8 memory probe, both optional.

## 17:30 — §7 TopMag prune on CSA indexer @64k COMPLETE → **STRONG GO**

- **What:** The two §7 legs from 16:50 finished; both 250/250 samples. Official verdict via
  `sg_capture.py report-prune --label 64k` (5 hardest tasks × n=50, dense re-measured in-run).
- **How:** `out/ruler_csa_idx_prune50_64k.json` + `_70_64k.json`; both logs end with `[acc] wrote
  ... in 35.5min / 35.9min` + `[unpatch] restored`.
- **Result (STRONG GO):** MEAN dense 0.883 → pr50 0.874 (**Δ50 = 0.82pt**) → pr70 0.877
  (**Δ70 = 0.55pt**); R50 0.967, R70 0.861. Per-task Δ50/Δ70 (pt): qa_2 **−2.0/−2.0**,
  qa_1 −3.0/−1.0, fwe +0.7/0.0, vt 0.0/0.0, niah_multivalue +0.25/+0.25.
  **Headline: the qa_2 caveat does NOT recur on the indexer** — latent qa_2 @70% was −4.5pt @64k,
  indexer qa_2 @70% is −2.0pt. Indexer 128-dim keys survive 50/70% TopMag more gracefully than the
  512-dim latent. Caveat: per-task n=50 → ~±7pt binomial noise (qa_1 −3.0@50 vs −1.0@70 is noise;
  its dense baseline varied 0.81-0.82 between configs).
- **Blockers:** Bash safety classifier transiently unavailable (did not block the verdict). Orphan
  sglang worker cleanup pending.
- **Next:** writeup — fill §7 in experiments.md + append indexer section to mustafar-sparse.md
  (draft first, needs approval). Optional: 32k indexer resume (13 tasks × n=100) still not run.

## 16:50 — §7 TopMag prune on CSA indexer @64k LAUNCHED (5 hardest × n=50, 8 GPUs)

- **What:** Full go/no-go for experiments.md §7 — TopMag magnitude pruning of the CSA **indexer**
  (128-dim keys, `--prune-target indexer`), not the 512-dim compressor latent. 64k RULER, the 5
  hardest tasks (qa_2, qa_1, fwe, vt, niah_multivalue) × n=50 = 250 samples/config, keep {0.5, 0.3}.
- **How:** Two concurrent `run-acc --prune-keep {0.5,0.3} --prune-target indexer --lengths 64k
  --n-64k 50 --tp 4 --mem-fraction 0.95` legs on 8 GPUs (0-3 keep0.5 MASTER_PORT=29501, 4-7 keep0.3
  MASTER_PORT=29500), via new launcher `transferibility/sg_prune_idx_64k_par.sh`, container
  **ruler-eval** (sglang-44039 no longer exists). setsid nohup → detached, survives session death.
  Out: `out/ruler_csa_idx_prune50_64k.json` + `_70_64k.json` (per-sample incremental + auto-resume).
- **Result (in progress):** ~10 samples/min per leg; qa_2 40/50 @4.3 min elapsed; all 8 GPUs
  ~72 GB. Runtime estimate ≈ 32–33 min wall, both legs in parallel (finish ~17:20).
- **Blockers / fixes:** (1) launcher v1 had the per-leg log redirect *outside* the docker exec
  bash -c string — host shell can't write `/mnt/host_root/...`, so legs never launched; fixed by
  moving `> $CT/log 2>&1` inside the string (as sg_prune_par.sh does). (2) container
  `sglang-44039` gone → retargeted launcher to `ruler-eval` (same v0.5.15 image, mounts + model
  path verified). (3) Bash auto-mode safety classifier intermittently unavailable (transient).
- **Next:** report-prune verdict on the two out files; check whether the qa_2/qa_1 @70% caveat
  (latent: qa_2 −4.5 pts @64k, qa_1 +2.0) recurs when the indexer is the pruned object; update
  writeup §7.

## 10:54 — Two planned experiments recorded: xKV low-rank + TopMag sparsity on the CSA indexer

- **What:** Added sections 6 and 7 to `writeup/experiments.md` capturing two *planned* (not yet
  run) experiments: (6) xKV cross-layer low-rank on the CSA indexer; (7) TopMag (Mustafar)
  magnitude sparsity on the CSA indexer. Both marked `Status: planned — not yet run`; no results
  fabricated (headline-table rows `*planned*`).
- **Why:** the CSA indexer (per-query top-512 selection over 64 index heads, fp32
  `[B, S_q, 64, S_kv/4]` score tensor) is likely the **largest non-MoE compute component** in
  V4-Flash — the two methods that proved out on the compressed latent (free @32k / 6.4× cut for
  xKV; STRONG GO pr50/pr70 for TopMag) are worth applying there, where the win is **compute**
  (dims/token, skipped score work), not KV bytes.
- **Design (as written):** §6 — same 2-pass capture → joint SVD → inject as the latent study,
  W3@b64 shared rank-192 basis across CSA layers, end-task RULER @32k × n=100 vs native. §7 —
  Section 4 harness (`run-acc --prune-keep {0.5,0.3}`) retargeted from the latent to the indexer,
  same go rule (mean drop ≤ 2 pts, R(0.5) > 0.90), plus whether the qa_2 @70% @64k caveat recurs.
- **Next:** run both experiments; on completion fill in the real numbers in `experiments.md`
  sections 6/7 and move the headline rows out of `*planned*`.
