# Log — 2026-09-03

## 19:15 — UNTOUCHED 0731 dequant TP8 baseline killed (pid 86471) after ~1.5-h DeepSWE eval leg

- **What:** User commanded "kill the server." Killed the untouched 0731 dequant baseline (port 30211, log `serve_untouched_0731_dequant_tp8_500k_bs8_30211.log`) and verified all 8 GPUs free (0 MiB) and port 30211 released. Stopped the error-monitor cron fa25d89a (server gone).
- **Eval leg stats while it ran:** DeepSWE harness (10.57.3.76) hit the server 09:42:57 → last POST 11:10:36 UTC. **2,315 `POST /v1/messages?beta=true`, all 200 OK, 0 non-200, 0 errors** (no tracebacks/OOM/context-overflow/assertions) across the whole log. Decode on CUDA graphs throughout; long ~100k-token decode at ~133 tok/s; eval went quiet ~11:10 UTC (idle when killed). The eval was still quiet (possibly between tasks) at kill time — any partial task results live on the eval host.
- **Result comparison note:** untouched leg `max_total_num_tokens=4,924,672` vs packed leg 5,965,056 → **1.211× KV capacity from packing** on TP8 (matches TP4 writeup's 1.2119×).
- **Next:** user's call — relaunch (packed or untouched) for further legs, or free the GPUs.

## 17:38 — UNTOUCHED 0731 baseline relaunched on TP8 (official checkpoint, fp4→fp8 dequant, NO TopMag / NO packing)

- **What:** Per user request, killed the packed TopMag 0731 server (pid 67909, log `serve_packed_graph_tp8_500k_bs8_30211_0731.log`) and relaunched a **fair untouched baseline** for the DeepSWE comparison: the official `/mnt/public_data/deepseek-ai/DeepSeek-V4-Flash-0731` served with **no TopMag env vars** (`SGLANG_OPT_TOPMAG`, `XKV_TOPMAG_KEEP`, `SGLANG_OPT_TOPMAG_PACKED_C4` all absent) and no packed C4 — but **with `SGLANG_DSV4_FP4_DEQUANT=1`**, so the only difference vs the packed leg is the packing itself.
- **How:** new launcher `mustafar/scripts/local/launch_inner_untouched_0731_dequant_tp8_500k_bs8_30211.sh` (log `logs/serve_untouched_0731_dequant_tp8_500k_bs8_30211.log`, pid 86471, port 30211, MASTER_PORT 29628). Identical serving config to the packed leg: tp8, ctx 524288, fp8-gemm-backend triton, decode CUDA graphs full bs[1,2,4,8], prefill disabled, mem-fraction 0.93.
- **Why dequant (not fp4-native):** the user's initial "untouched, no fp4-dequant" choice was tried first. A pure-fp4-native launch (no dequant, `moe_runner_backend=auto`) **crashed at decode-CUDA-graph capture**: `AssertionError: Hidden size mismatch` in `fused_moe.py` — auto falls through to Fp8MoEMethod + the fp8 triton runner, which can't execute fp4-packed weights. fp4-native requires `--moe-runner-backend flashinfer_mxfp4` (→ `Mxfp4FlashinferCutlassMoEMethod`, the fork's documented Hopper path) or `marlin`. But running the baseline fp4-native while the packed leg dequants would confound the comparison with a kernel-path difference. User confirmed: **run the baseline in dequant too, for a fair apples-to-apples comparison** — the dequant is exactly lossless (fp4 ⊂ fp8 exactly; ue8m0 scale is a power of 2 → fp4×scale exactly fp8-representable), so the expert math matches the packed leg bit-for-bit; only the packing differs.
- **Result:** booted 09:33:59 UTC → ready 09:37:52 UTC (~4 min, incl. load + graph capture). `Dequantized FP4 expert weights to FP8.` on all 8 ranks. Decode graphs bs[1,2,4,8] captured (~11 s). **`max_total_num_tokens=4,924,672`** vs packed leg's 5,965,056 → packing = **1.211× KV capacity**, matching the TP4 writeup's 1.2119×. Health 200; smoke round-trip returned `UNTOUCHED0731_OK`; zero errors in log.
- **Error-monitor:** cron repointed from the packed log → `serve_untouched_0731_dequant_tp8_500k_bs8_30211.log` (new job fa25d89a, offset `/tmp/serve_err_watch_offset_untouched`).
- **Next:** launch the DeepSWE untouched leg against this baseline when the user is ready (same harness config as the packed 9-02 leg, ANTHROPIC_BASE_URL=http://10.72.1.175:30211).

## 12:15 — Packed TopMag TP8 server restarted onto the OFFICIAL 0731 checkpoint (fp4 experts dequantized to fp8)

- **What:** Killed the packed Stage-1 TP8 server (pid 38169) that was serving the pure-fp8 `sgl-project/DeepSeek-V4-Flash-FP8` repack, and relaunched the identical packed config pointing at the **official `/mnt/public_data/deepseek-ai/DeepSeek-V4-Flash-0731`**. Motivation: get the packed TopMag leg serving the same dated checkpoint family as the untouched 7-31 DeepSWE baseline.
- **How:** New launcher `mustafar/scripts/local/launch_inner_packed_graph_tp8_500k_bs8_30211_0731.sh` (log `logs/serve_packed_graph_tp8_500k_bs8_30211_0731.log`, pid 67909, port 30211, MASTER_PORT 29626). Identical flags to the prior run — `SGLANG_OPT_TOPMAG=1 XKV_TOPMAG_KEEP=0.5 SGLANG_OPT_TOPMAG_PACKED_C4=1`, tp8, ctx 524288, fp8-gemm-backend triton, decode graphs full bs[1,2,4,8], prefill disabled — **plus `SGLANG_DSV4_FP4_DEQUANT=1`**.
- **Why the new env var:** the official 0731 ships **fp4-packed routed experts** (I8 containers + F8_E8M0 ue8m0 scales, auto-detected `is_fp4_experts=True`), unlike the pure-fp8 repack. `SGLANG_DSV4_FP4_DEQUANT=1` makes the runtime cast fp4→fp8 at load (`cast_e2m1fn_to_e4m3fn`, **value-preserving** — expert math identical to native fp4) and requires moe-runner-backend auto (default ✓), so the validated fp8-triton packed path runs unchanged.
- **Result:** booted 04:11:49 → ready 04:13:45 UTC (~2 min). Log shows `Auto-detected ... is_fp4_experts=True` then `Dequantized FP4 expert weights to FP8.` on all 8 TP ranks (04:13:23). Mustafar packed C4 pool up: layers=21, page_size=64, logical_row_bytes=328. Decode graphs `bs=[1,2,4,8]` captured (~10.5 s). `max_total_num_tokens=5,965,056` (slightly under the fp8-repack's 6,023,168 — native's weight footprint differs marginally). Health HTTP 200; smoke round-trip returned `PACKED0731_OK`. GPU mem ~76.5 GB/rank after boot.
- **Blocker / note:** the previous DeepSWE eval job (`task_20260903_104341_166`, in progress on the old fp8-repack server) was interrupted by this restart — user approved kill-and-relaunch. New eval on the 0731-backed server not yet launched (waiting on user).
- **Next:** relaunch the DeepSWE / bench leg against the 0731-backed packed server when the user is ready.
