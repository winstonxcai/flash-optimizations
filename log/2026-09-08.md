# Log — 2026-09-08

## 05:50 — HiCache × Remnant ladder COMPLETE: CPU-L2 equalizes both legs at c21 (fork-fix to packed c4 host mirror required)

- **What:** Drove the approved HiCache × Remnant LongSWE SLO-concurrency experiment to completion (report.md block written). Question: does Remnant still out-concurrence Native once both run under SGLang's hierarchical cache (GPU L1 ↔ CPU DRAM L2)? Locked config every run: `SGLANG_ENABLE_UNIFIED_RADIX_TREE=1` + `--enable-hierarchical-cache --hicache-ratio 2.75 --hicache-write-policy write_through --hicache-io-backend direct --hicache-mem-layout page_first_direct` (no L3). TP4, mem-frac 0.88, fp8 KV, `flashinfer_mxfp4`, 1M ctx, max-run 256, chunked 8192, GPUs 4-7, port 30212, fresh MASTER_PORT per boot. One fresh-boot 1200-s first-traffic window per concurrency.
- **Fork fix (packed+HiCache was broken):** the stock DSV4 HiCache assembler (`hybrid_pool_assembler.py`) mirrors the c4 pool as one contiguous native 584-byte page span, which would silently corrupt Remnant's three 328-byte packed fragments. New `mustafar/patches/hicache.py` injects `MustafarPackedHostPool` (per-layer pinned mirrors of packed_values/bitmaps/scales, whole-page row transfers indexed by full-KV token id exactly like native) + a `get_packed_buffers` branch in the assembler's `c4_host_pool` construction. First smoke boot reached HEALTHY (packed host mirror 20.71 GB: layers=21, host_pages=46983, slot_page_size=256) then crashed at the first `write_backup`: `NotImplementedError: "index_cuda" not implemented for 'UInt64'` — CUDA advanced indexing has no uint64 kernel, and the bitmap fragment is uint64. Fixed by routing bitmap transfers through `.view(torch.uint8)` byte views on both host and device (values/scales already uint8). Restored the assembler from `.mustafar.orig`, re-`patch`+`verify` (6 targets), re-booted healthy.
- **Phase-1 smoke gate (packed+HiCache, after fix):** 600-s c15 first-traffic leg, dir `lswb-replay/packed-hicache-smoke/20260908_024224` — server up the whole window, **0 crashes/tracebacks**, valid=True done=975 ttft_p90=8.13 s, L1 96.97%, L2 0.15%. PASS. (The first smoke attempt, dir `.../20260908_023343`, died ~60 s in on the uint64 gather — that is the crash the byte-view fix resolves.)
- **Ladder (rows; PASS = valid AND ttft_p90 < 10 s):**
  - Native+HC c15 PASS done=2098 ttft90=5.95 e2e90=16.12 L1=0.9731 L2=0.0025 uncached=7.40M (`native-hicache-r1`)
  - Native+HC c18 PASS done=2143 ttft90=8.34 e2e90=17.50 L1=0.9729 L2=0.0021 uncached=7.70M (`r3`)
  - Native+HC c21 PASS done=2205 ttft90=8.77 e2e90=20.23 L1=0.9708 L2=0.0033 uncached=8.18M (`r5`)
  - Native+HC c24 **FAIL** done=2061 ttft90=10.86 L1=0.9237 L2=0.0491 uncached=7.97M (`r6`)
  - Packed+HC c18 PASS done=1966 ttft90=8.86 e2e90=20.48 L1=0.9717 L2=0.0017 uncached=7.52M (`r2`)
  - Packed+HC c21 PASS done=1961 ttft90=9.32 e2e90=22.63 L1=0.9688 L2=0.0037 uncached=7.71M (`r4`)
  - Packed+HC c24 **FAIL** done=2041 ttft90=10.34 L1=0.9681 L2=0.0042 uncached=8.03M (`r7`)
- **Result:** Ceilings — Native+HC **21** (was 12 no-HC), Packed+HC **21** (was 15 no-HC). **C\*_Remnant+HiCache (21) is NOT greater than C\*_Native+HiCache (21):** HiCache lifts both modes above their own no-HiCache ceilings but equalizes them, absorbing the pool-capacity edge that gave Remnant +25% (12→15) without HiCache. Both breach at c24 (Native 10.86 s, Packed 10.34 s), so the tie is genuine; Packed degrades marginally more gracefully. L2 only engages once L1 churns — ~0.2-0.5% hit through every passing point, rising to 4.91% only at Native's failing c24 (L1 dropped to 92.4%), the exact regime the old pool-capacity edge lived in. Resolution +3; both failed the single step past 21.
- **Artifacts:** rows `mustafar/results/lswb-replay/{native,packed}-hicache-r{1,2,3,4,5,6,7}/20*/client/summary.json` (+`packed-hicache-smoke`, `native-hicache-sanity1/2`); boot logs `mustafar/logs/serve_{native,packed}_hicache.log`; copy of the ladder rows `/tmp/hicache-rows.txt`.
- **Next:** report.md `#### HiCache × Remnant (CPU-L2 cache)` block + 4-row final table written; commit serve.sh / hicache-ladder.sh / lswb-row.sh / hicache.py / config.py / patching.py / attention.py / report.md / log to flash-optimizations and push origin/main. Raw `mustafar/results/` stays untracked (artifact tree, not committed).
