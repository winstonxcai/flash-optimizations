# Log — 2026-08-27

## 12:23 — Windowed self-fit W=4096: first clean Sangfor-Bench eval → 19/29, coherent patch

- **What:** Ran the first Sangfor-Bench smoke eval on the windowed self-fit lowrank build
  (`XKV_WINDOWED=1`, `XKV_WINDOW=4096`, `XKV_COEFF_DIM=192`; per-window rank-192 SVD fit on each
  window's own normed latents, 528 B/slot). Single instance
  `gcjs_kube-log-check-recover_2cadb18b` (MaxInterval restart + Chinese-log task) at
  max_workers=1. Model server on 30211, tp=4, mem-fraction 0.93, mrr 64, chunk 4096.
- **Result:** run_agent **19/29 (65.5%), resolved=false** vs native CSA **29/29 (100%)** on the
  same instance. First lowrank eval that produced a coherent, task-appropriate patch (contrast:
  fixed-basis = 10/10 garbage). Agent read the right file, planned the correct MaxInterval
  fix, applied the core edit; full 32-min agent loop clean, server healthy (420 win_finalize,
  0 server errors).
- **Failures (10), all root-caused:** 8× B-series = sleep-path log `"距离上次重启时间达到SleepInterval，重启Pod"`
  missing 间隔 vs test's `"距离上次重启时间间隔达到SleepInterval，重启Pod"` (return value correct);
  2× DeletePod-log = Edit#2 failed to apply (`String to replace not found`), `"更新重启事件"` log
  never added. 19 passes include all 9 MaxInterval A-tests + 4 C-tests → core logic correct.
- **Next:** more samples if a resolve-rate signal is wanted; consider whether exact-string log
  fidelity is a compression-sensitive axis worth probing (A/B with native on string-precision
  tasks). Writeup: `writeup/lowrank-sangfor-windowed-self-fit-first-eval.md`.
