# Log — 2026-09-01

## 21:30 — Stage-1 packed (328B) 25-task Sangfor eval launched on two TP4 servers

- **What:** Launched the Stage-1 **packed** TopMag50 eval (real 328-byte C4 compression, not the earlier dense-zero TopMag50) on 25 NEW Sangfor-Bench tasks at `--context-length 262144`, using all 8 H100s split 4/4. Native baseline not rerun — compare packed vs existing cloud-native.
- **How:**
  - Patch: `compressor_v2.py` had only the OLD dense-zero stub (8 lines, `## MUSTAFAR` markers present but no `pack_c4_rows`). `git checkout` restored pristine; `python3 -m mustafar patch` applied the full 5-file packed patch (compressor, memory_pool, pool_configurator, indexer, backend). Verified `packed_c4_enabled`/`MustafarPackedC4KVPool` in source; `packedselftest` + `gpu_packed` OK on GPU (`logical_bytes_per_row=328`, `compression=1.7805`, `native_store_exact:true`, `no_native_shadow_pool:true`).
  - Servers: A = GPUs 4-7 / port 30211 / MASTER_PORT 29624 / config `docker_env_config_dsv4-windowed.json`; B = GPUs 0-3 / port 30212 / MASTER_PORT 29623 / config `docker_env_config_dsv4-windowed-30212.json`. Both `SGLANG_OPT_TOPMAG=1 XKV_TOPMAG_KEEP=0.5 SGLANG_OPT_TOPMAG_PACKED_C4=1`, ctx 262144, mem-frac 0.93, disable-cuda-graph.
  - Tasks: `select_packed25.py` on 10.57.3.76 — cloud-native outcomes (`task_20260825_195126_744`), exclude the 25d prior set, `MIN_TESTS>=8`, even pass/fail: hard 4P/4F, medium 5P/5F, easy 4P/3F (=13P/12F; 8/10/7 difficulty). Runner `run_packed_25_262k.sh` pre-assigns alternating A/B (13/12) and runs both chains concurrently; order hard→medium→easy; RID `dsv4-packed-262k-<task>_20260901`; master log `results/dsv4-packed-262k_master.log`, markers `PACKED_START`/`ALL_25_DONE`.
- **Result:** Both servers show `Mustafar packed C4 pool: layers=21 page_size=64 logical_row_bytes=328` and `DSV4 pool sizes: full=402688` (packed capacity). Remote runner started 21:11:59, both chains live (`[A] apex_chat-agent_9347a21`, `[B] apex_chat-agent_f268ef1`), both servers actively prefill/decode (all 8 GPUs ~77-78 GB). Watchdog `watchdog_packed_262k.sh` armed (10-min poll, 48h cap, exit 2 on either server down).
- **Blockers / fixes:** scp stripped exec bit → chmod +x; nohup+ssh fd-holding keeps one ssh session alive (harmless). Classifier intermittently timing out on Bash.
- **Next:** Poll master log until first DONE lines land (~40-60 min/task, 2 concurrent → est. 8-12 h total); then collect per-task `test_result.json`, fold packed-vs-native into writeup table.
