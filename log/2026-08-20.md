# Log — 2026-08-20

## 11:16 — Experiment 8: composed xKV W3 recon → TopMag50 → CSA indexer @64k COMPLETE → **STRONG GO**

- **What:** Composed the two indexer compute levers end-to-end on the CSA indexer's 128-dim keys:
  `K^I →(xKV W3@r192)→ K̂^I →(TopMag 50%)→ K̃^I →(indexer)→ Top-512`. Reconstruct cross-layer
  (rank-192 shared basis per group of 3 CSA layers, 2:1), then TopMag50 the *reconstructed* keys
  (mask on `|R_slice|`, zero the raw `inv` coords so the store renormalizes into `TopMag(K̂^I)`),
  then the ordinary indexer selects. The exp-6 W3-only and exp-7 TopMag50 runs become **baseline
  columns** of exp-8's report (per user: "this combined version should be experiment 8").
- **How:** `sg_capture.py` — new `composed` block in `on_compress` (after the W3 inject, before the
  native-prune guard, which is now excluded under the composed config), pass-1 mode fix
  (compose needs the W3 capture, so `recon_target=="compressor"` is required for plain-dense pass 1),
  composed-mode launch note, entry-dict `prune_target`/`recon_target` fields, `compose50` progress
  label. Single tp=4 leg on **4 GPUs** (user: only 4 available now) via new launcher
  `transferibility/sg_idx_w3_tm50_64k.sh`, container `ruler-eval`, 64k, 5 hardest tasks × n=50 =
  250 smp, 35.9 min, clean unpatch.
- **Validation:** smoke (32k, n=3, `XKV_DEBUG=1`) — 504 `inject` ↔ 504 `compose_inject` (1:1),
  **zero** `prune_inject` / `compose_skip` / `dim=512`; R(0.5)=0.968. Regression guard: exp-7 path
  (252 `prune_inject`, 0 compose) and exp-6 path (252 `inject`, 0 compose) — both byte-identical.
- **Result (STRONG GO):** composed MEAN **0.883** vs its paired native 0.869 (**Δ = −0.014**, i.e.
  composed 1.4 pts *above* native on the same 250 samples), and ≥ both single levers (W3-only
  0.877, TopMag50 0.874). vs historical native 0.883–0.888 within 0.5 pt. Only 11/250 samples flip
  (+7/−4 net); fwe still carries the small word-recall cost (0.833, 3 down), retrieval/needle free
  (niah 1.000, vt → 1.000). **The errors do NOT compound** — hypothesis: SVD-truncation error sits
  in the smallest-|·| coords, which TopMag50 zeroes → pruning the reconstruction partially cleans
  the W3 error.
- **Caveats:** n=50 → ±4.8 pts/column (qa_2 −0.08 "gain" is within noise); this run's dense drew
  low (0.869 vs 0.883–0.888, qa_2 0.72/qa_1 0.78) — shared-gt dense matches 234/242 across runs so
  the native path is stable, the difference is sample draw + fp8 nondeterminism; 4× score-cost
  ceiling is kernel-gated (needs a sparse fused indexer kernel).
- **Next:** writeups §8 (`experiments.md`) + Part 5 (`xkv-crosslayer.md`) written and pushed; log
  committed. Open/optional: 32k indexer TopMag resume (13 tasks × n=100) and the 256k/tp=8 memory
  probe.
