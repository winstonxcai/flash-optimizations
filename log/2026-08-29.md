# Log — 2026-08-29

## 23:15 — 25-distinct TopMag50 eval: Phase A prepared + autonomous GPU poll armed

- **What:** 25-distinct Sangfor-Bench generalization eval of TopMag50-on-native-c4 prepared (plan `web-soft-forest.md` approved). Even native pass/fail split (13P/12F), difficulty varied (7 easy / 10 medium / 8 hard, 10 go / 15 python). Sequential per-task runs on the 30211 tp=4 server, each sample a separate yjybench invocation with a unique `dsv4-topmag50-25d-<task>_20260829` run_id.
- **Files:** `flash-optimizations/mustafar/scripts/run_topmag50_25d.sh` (25-row task table, `MASTER_START`/`ALL_25_DONE` markers, master log `dsv4-topmag50-25d_master.log`), `watchdog_topmag50_25d.sh` (10-min poll of 30211 server + remote master log; 48 h cap vs 12 h before), `launch_when_free.sh` (idempotent gate: exits 0 if already running/done/launched, exit 1 while waiting for ≥4 GPUs with ≥75 GB free, then runs `launch.sh` → scp run script to 10.57.3.76 → remote `nohup` → background watchdog).
- **Fixed:** RID collision — the inherited `cut -c1-24` made all 4 `gcjs_kube-log-check-recover_*` instances collapse to the same truncated RID. Now uses the full sanitized instance id; verified 25/25 unique (max 63 chars).
- **Poll:** durable CronCreate `5474be0a` (`1-56/5 * * * *`) runs `launch_when_free.sh` every 5 min, reports on exit 0, self-deletes on LAUNCHED/complete, silent on exit 1. Auto-expires after 7 days.
- **Blockers:** huangz's `ruler_32k_eval.py` still holds all 8 H100s (~5.8 days ETA from 22:57 measurement) → launch gated on GPUs. Git classifier outage (deepseek-v4-flash "temporarily unavailable") intermittently blocked Bash writes; worked around by splitting git into single commands + `git -C <repo>` (cwd had drifted to `mustafar/scripts/`).
- **Next:** poll every 5 min; on 4 free GPUs launch Phase B autonomously (server → remote eval → watchdog); monitor `dsv4-topmag50-25d_master.log` for `ALL_25_DONE` (~18–20 h expected once launched).
