# Log — 2026-08-31

## 14:45 — NATIVE-CSA baseline rerun LAUNCHED at --context-length 262144 (GPUs 0-3) — both 262k legs now parallel

- **What:** Native leg of the context-fix rerun (`SGLANG_OPT_TOPMAG=0 XKV_TOPMAG_KEEP=1.0`), tp=4, GPUs 0-3 (td69032's 0731 server ended — 0-3 fully free again), **port 30212**, `--context-length 262144`, MASTER_PORT 29623. Same 8 hard tasks + same harness.
- **How:** new `launch_inner_native_262k.sh`; remote config `docker_env_config_dsv4-windowed-30212.json` created via `cp`+`sed` (30211→30212, never echoed), base URL verified masked = `http://10.72.1.175:30212`; `run_hard_native_262k.sh` scp'd, `HARD_START 2026-08-31 14:38:13`, task 1 `sri_swe-bench_5f5a7df7` running (server log shows cached-token 24.5k growing). Watchdog armed (bg `b718otnau`). Server ready ~06:36, full_token=332288 → 262k fits.
- **Result:** pending — this is the apples-to-apples partner to the TopMag50@262144 leg (started 10:53:53, currently task 2/8) so the cloud-vs-local crater can be closed for BOTH.
- **Blockers:** ssh launch hung on backgrounded fd (timeout-killed after 25s; launch confirmed via master log).
- **Next:** both watchdogs → ALL_8_DONE; build 4-way table: cloud-native(1M) vs native@262k vs TopMag@262k vs TopMag@135k. Two servers on 8 GPUs (30211/4-7 + 30212/0-3) co-serve; do not disturb.

## 11:00 — TopMag50 8-hard rerun LAUNCHED at --context-length 262144 (GPUs 4-7)

- **What:** The cloud-vs-local investigation concluded the ~104k-input crater was sglang's 135168−32000 completion budget = ~103,168 effective input ceiling. This rerun removes the wall: TopMag50-on-native-c4 (`SGLANG_OPT_TOPMAG=1 XKV_TOPMAG_KEEP=0.5`), tp=4, GPUs 4-7 (0-3 are owned by td69032's active sglang 0731 server, port 33464 — untouched), port 30211 (reuses `docker_env_config_dsv4-windowed.json`), `--context-length 262144`, MASTER_PORT 29624. Same 8 hard tasks + same yjybench harness as `run_hard_native.sh`.
- **How:** new scripts `launch_inner_topmag_262k.sh` / `run_topmag50_hard8_262k.sh` / `watchdog_topmag_hard8_262k.sh`; server ready (~2:50, full_token=332288/GPU → 262k fits pool), remote eval `HARD_START 2026-08-31 10:53:53`, task 1 `sri_swe-bench_5f5a7df7` running (server log shows ctx ~72k full tokens, gen ~7 tok/s). Watchdog armed (bg `bdifaxpgs`, polls 600s, 48h cap).
- **Result:** pending — compare vs cloud-native 60.8% (ALL 8) / 55.2% (ex-degen) and vs TopMag50@135168 43.2% / 35.1%.
- **Blockers:** classifier timed out on the watchdog-arm (worked via Bash run_in_background); ssh launch hung on the backgrounded fd (eval confirmed started via master log).
- **Next:** watchdog → ALL_8_DONE (~7h est. given 5h23m measured per leg + longer swe-bench trajectories at the unwalled window); then build the 3-way comparison + fold into writeup.

## 09:04 — Hard-native control DONE: the hard-bucket crater is a CLOUD-vs-LOCAL artifact, not TopMag

- **What:** 8-hard-task local NATIVE-CSA control rerun (`dsv4-hardnative-*_20260830`) on GPUs 0-3 finished `ALL_8_DONE 2026-08-31 06:09:17` (native server, `SGLANG_OPT_TOPMAG=0`, watchdog armed pid 4190919, autopilot stopped after launching — wedged on the ssh backgrounded-fd hang). 3-way comparison: cloud-native (20260825 run) vs local-native vs TopMag50.
- **Result:** cloud-native **60.8%** → local-native **40.1%** (server/noise effect **−20.7pp**, −23.7pp ex-degen) → TopMag50 **43.2%** (+3.1pp vs local-native, +3.5pp ex-degen). Per-task TopMag-vs-local: 5 identical, 1 regress (−9.1pp `sri_s1_cec32c82`), 2 improve (`sri_swe-bench_fea293e6` +30.2pp, `sri_ap-gpt_2bcf1160` +6.1pp). The pass-bucket "leaks" (`sri_swe-bench_5f5a7df7` 100→36, `fea293e6` 100→73) crater on LOCAL NATIVE too (100→40, 100→43) → not pruning.
- **Interpretation:** the earlier −17.7pp "hard crater" from the 25-distinct run was a **cloud-vs-local artifact**. TopMag50-on-native is native-equivalent (slightly better) on the hard bucket when compared against the same local server. Remaining un-controlled confound: medium pass-bucket leaks (`fy_gptanalystagent` 111/111→55, `sri_s1_00ce55e2` 126/126→93) have no local-native control.
- **Next:** kill idle native server (frees 0-3); update writeup with the 3-way hard table + LB2 lossless table (needs approval); commit run scripts.

## 00:41 — 25-distinct TopMag50 eval COMPLETE (`ALL_25_DONE`)

- **What:** Sangfor-Bench TopMag50 25-distinct run on 0-3 finished all 25 tasks (`ALL_25_DONE 2026-08-31 00:41:46`). Autopilot then fired the hard-native gate (00:43:35: killed TopMag server, launched native server, ready=1, remote HARD_START 00:45:49).
- **Result (per-task pass-rate, ex-degen):** ALL 24: native 75.4% → TopMag 71.8% (**−3.6pp**). easy+medium (17): 83.6→86.9 (**+3.2pp**). hard (5): 55.2→35.1 (**−20.2pp**). native-PASS bucket (12): 100→88.1 (4 leak). native-FAIL (12): 50.7→55.4 (+4.7pp, incl huge swings 0/62→62.9, 101/254→96.9). Large two-sided variance on agentic n=1 → cloud-vs-local confound suspected; resolved by the hard-native control (see above — crater was server effect).
- **Next:** fold complete tables (25-distinct + hard 3-way + LB2 lossless) into the writeup.
