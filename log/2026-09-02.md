# Log — 2026-09-02

## 20:40 — Rebuilt decode graphs to bs8 (fix conc>2 cliff); eval resumed

- **What:** The deepswe eval workload (~5 concurrent 128-300k requests) was running **off-graph**: decode graphs only covered bs[1,2], so batches >2 fell to eager (~7 tok/s/stream, ~35 tok/s aggregate). Per user's "this is way too long", rebuilt the server with decode CUDA graphs **`max_bs:8, bs:[1,2,4,8]`** so 4–8 concurrent requests all decode on-graph.
- **How:** New launcher `mustafar/scripts/local/launch_inner_packed_graph_tp8_500k_bs8_30211.sh` (log `logs/serve_packed_graph_tp8_500k_bs8_30211.log`, MASTER_PORT 29626). Same packed config, ctx 524288, pool 6,023,168. Killed old server, relaunched, ready 12:37 UTC (100s).
- **Verified fix:** conc-6 probe at 128k→2048 (np6): **Output token throughput 200.5 tok/s** vs **39.7 tok/s** on the old bs2 graph → 5× aggregate; TPOT 18.7 ms (~53 tok/s/stream × 6). Confirmed `cuda_graph_config=...decode=max_bs=8, bs=[1,2,4,8]` in the ServerArgs line.
- **Note:** after the earlier crash, no recurrence in ~2h of clean serving (893 requests 200 OK pre-rebuild). Eval restarted by user immediately after this rebuild.

## 18:29 — TP8 server crashed (CUBLAS in compressor compute_kv_score); relaunched

- **What:** Server (packed tp8, ctx 524288, port 30211) crashed at 10:27 UTC (18:27 local) mid-eval. Deepswe workload had reconnected and was serving ~147 requests 200 OK; decode was running on-graph at 128 tok/s (running-req 1, 132k ctx) when a new request's **prefill** hit the fault.
- **Crash:** `RuntimeError: CUDA error: CUBLAS_STATUS_EXECUTION_FAILED` in `compressor_v2.py:520 forward_unified → compute_kv_score → linear_bf16_fp32 → torch.mm` (dsv4/jit_kernel gemm.py:24). Rank 0 died (SIGQUIT, "one child failed"); ranks 1–7 hung at 100% util with ~80GB still allocated; only GPU 0 freed.
- **Differential:** dmesg Xid 31 MMU faults (kernel-time 1642701/1648604) are 1.7–3.3h OLD — predate this server's 08:49 UTC start, so they're from an earlier job, NOT this crash. **No fresh GPU hardware fault** → looks application-level in the TopMag store-time compressor GEMM, not a GPU dying.
- **Recovery:** `pkill -9` launch_server in ruler-eval; all 8 GPUs freed (0 MiB, 81 GB each). Relaunched same config (pid 31986), ready 10:33 UTC (85s). Health 200, max_model_len 524288, smoke round-trip 0.37s. Monitor offset reset to fresh-log size.
- **Risk / next:** if the crash recurs in `compute_kv_score`, it's a real TopMag compressor bug (needs isolated repro at the failing prefill shape), not a one-off. Watch the live monitor.

## 17:10 — TP8 (ctx 524288) packed server relaunched; 256k→4096 speed bench done

- **What:** Killed the TP4 packed server on 30211 (ctx 196608, pool 402,688 slots) and relaunched on **TP8 with ctx 524288** to cover the deepswe 423k-input tail + 32k completion (config-coverage: 524288 = 100% of historical trials). All 8 H100s, port 30211.
- **How:** `mustafar/scripts/local/launch_inner_packed_graph_tp8_500k_30211.sh` — same packed flags (`SGLANG_OPT_TOPMAG=1 XKV_TOPMAG_KEEP=0.5 SGLANG_OPT_TOPMAG_PACKED_C4=1`), decode CUDA graphs FULL bs[1,2], prefill disabled, `--chunked-prefill-size 4096`, `--mem-fraction-static 0.93`, MASTER_PORT 29625, log `logs/serve_packed_graph_tp8_500k_30211.log`. Ready 08:49:30.
- **KV pool:** `max_total_num_tokens=6023168` → **6,023,168 slots (15× tp4 packed 402,688)**; packed C4 pool 10.4 GB (pages/layer=23529). Covers 500k+ and even 1M configs. Decode graphs cap at batch 2 → concurrency ≤2 for full graph speed.
- **256k→4096 bench** (`bench_256k4096_tp8.sh`, `results/official-256k4096/`): c1 (conc1,np2): decode TPOT **7.84 ms ≈ 127.5 tok/s**, TTFT 16.5 s @256k (≈15.9k tok/s prefill), E2E 48.6 s. c2 (conc2,np4): output throughput **131.9 tok/s** aggregate, TPOT 9.88 ms, TTFT 21.6 s, E2E 62.1 s. Pool only ~17% used at 4×256k.
- **Fixes:** random-dataset bench failed first (ShareGPT fetch to huggingface.co → connection reset; no HF cache in container). Fixed with `HF_ENDPOINT=https://hf-mirror.com` (mirror reachable, hf.co blocked).
- **Next:** error-monitor cron repointed to the tp8 log; deepswe / second consumer may reconnect against the bigger advertised window.

## 09:40 — Stage-1 packed 25-task run complete; results collected; servers killed

- **What:** The two TP4 Stage-1 packed (328 B) Sangfor servers finished all 25 tasks (`ALL_25_DONE` 05:16:49, ≈8 h, all rc=0). Collected + verified per-task `test_result.json` for all 25 against cloud-native (`task_20260825_195126_744`). Writeup fold-in **held off** per user; results live in the log only for now. Both servers killed; all 8 GPUs freed.
- **Result (packed vs native, weighted Σ passed/Σ total):**
  - **ALL 76.5 vs 82.3 = −5.8 pp**; hard −12.3, medium −3.6, easy **+12.8**.
  - Two **genuine** severe regressions (same-name test flips, not suite change): `apex_chat-agent_f268ef1` 32/32→8/32 (24/32 flips), `aiyycp_sales-flow_09299ad2` 24/24→7/24 (17/24 flips).
  - Excl. those two: **−0.1 pp** (80.8 vs 80.9); hard −6.6, medium +2.9, easy +12.8.
  - **5 native-fail tasks fixed to full pass** (9207ca23, ab21ecf0, edb6ec00, 76e6e4f8, 8073b35d); 191d12be improved 16/37→24/37. 13 tasks ≥100% under each leg (8 overlap; 5 swap out / 5 swap in).
  - Same-suite test-case deltas (22 rows): 6 +tc / 6 −tc / 10 flat. Suite-size changed on 3 rows (1dbcd396 111→55, 2439f30d 24→20, ef78d2c0 15→30).
- **Caveats:** native = cloud run, no local-native control leg — the 2 severe regressions need a local-native rerun to fully isolate packing vs cloud/prompt noise (flip sizes are far outside §3d noise band, so very likely real).
- **Artifacts:** aggregator `flash-optimizations/mustafar/scripts/local/agg_packed25.py` (committed eb07fc0, pushed). Result dirs `/data/zyj/YJYBench/results/test/Sangfor-Bench_cc_vibe_deepseek-v4-flash_dsv4-packed-262k-<task>_20260901/`; master log `/data/zyj/YJYBench/results/dsv4-packed-262k_master.log`; server logs `mustafar/logs/serve_packedA_262k.log` / `serve_packedB_262k.log`.
- **Next:** pending user — writeup §3e fold-in (held off); optional local-native rerun of f268ef1/09299ad2 to confirm the packing attribution.
