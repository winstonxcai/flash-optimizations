# Log — 2026-09-07

## 10:50 — Bench entrypoints unified onto bench-serving.sh; underscore bench_serving.sh deleted; serve.sh env names fixed

- **What:** Per user decision, made `mustafar/scripts/local/bench-serving.sh` (hyphen) the single canonical bench driver with two interfaces: the existing report-grade `fair|max <ctx> [C_fair]` dual-leg container protocol (unchanged, `bench-fair.sh`/`bench-max.sh` shims intact), plus a new standalone single-config mode `native|packed|fused <in> <out> <concurrency>` that self-boots its own report-config server (no container/env.sh) — the interface Modal `app.py::bench_serving`, `tests/test_bench_serving.py`, and the Dockerfile/docs now point at. Deleted the refactor's parallel `bench_serving.sh` (underscore).
- **Standalone measurement config aligned to the local report protocol** (user decision): fp8_e4m3 KV, mem-frac 0.88, 1M ctx, 256 max-run, 8192 chunk, extended decode graphs (max_bs 136, prefill off), DeepSeek parsers; warm-up wave of C then **3C** measured waves (not underscore's single-wave/0.90-mem/no-fp8/16-graph setup). Env knobs (PYTHON, RESULTS_DIR, PORT→30211, SEED→42, MEM_FRAC/CTX_LEN/MAX_RUN/CHUNK/DECODE_CFG/MODEL_NAME) overridable. Args validated rc2 before startup; concurrency cap 136; MODEL_PATH required; --help rc0.
- **serve.sh env fix (would otherwise run inert on the refactored runtime):** packed leg now sets runtime names `SGLANG_OPT_TOPMAG=1 KEEP=0.5 SGLANG_OPT_TOPMAG_PACKED=1 SGLANG_OPT_TOPMAG_FUSED=0`; native sets all off explicitly; legacy `XKV_TOPMAG_KEEP`/`SGLANG_OPT_TOPMAG_PACKED_C4` unset in-container (config.py reads only the new names).
- **Verification (no GPU):** `bash -n` on both drivers; `--help` rc0 lists both interfaces; bad-arg cases (bogus mode, input 0, conc 137>136, conc auto, fair missing ctx) all rc2; conc 136 passes arg gate → dies on missing MODEL_PATH rc1. `test_bench_serving.py` shell tests (syntax/help, bad-args, fixture) pass 3/3; the 2 modal-import tests can't run here (`modal` not installed — user OK'd skipping). `git grep bench_serving.sh` → only archive/history; hyphen wired into Dockerfile, modal readme, local README, app.py, tests.
- **Uncommitted (in flash-optimizations, on top of user's dirty `.gitignore`/`env.sh`/`lb2_serve_eval.py` LongBench-v2 path move):** `Dockerfile`, `local/README.md`, `local/bench-serving.sh`, `local/serve.sh`, `modal/app.py`, `modal/readme.md`, `tests/test_bench_serving.py` modified; `bench_serving.sh` deleted. Not committed/pushed.
- **Next:** when 4 GPUs are next free, one `bench-fair.sh` smoke validates the serve.sh env fix + 3C protocol on the refactored runtime; report.md numbers unaffected (they predate the refactor).

## ~10:45 — Reconcile with user's codex refactor (PR #1); scripts cleanup collapsed to .gitignore (37b4edb)

- **While I was doing "remove top-level one-off scripts, move config.json into scripts/local/", origin/main had advanced via user's own PR #1** (`codex/stage2a-fused-reconstruct`, merge e4c6888): a code-quality refactor ("core functionality unchanged") that removed all 8 top-level `scripts/*` one-offs, deleted `scripts/config.json`, deleted `scripts/local/{agg_packed25.py,select_packed25.py}`, renamed `packed_c4.py`→`packed.py`, replaced `ops.py` (571 lines) with `patches/{attention,compressor,pool}.py`+`patching.py`, added `fused.py`, `cuda/{fused.cu,bindings.cpp,packed_abi.cuh,setup.py}`, new `tests/*` (test_refactor, test_numerics, test_backend_selection, fused_cpu/gpu), new `scripts/local/bench_serving.sh` + `tests/test_bench_serving.py`, rewrote `config.py`/`__init__.py`/`modal/app.py`; env.sh now points only at the eval-box `docker_env_config_dsv4_0731.json`.
- **My local commit became redundant** (deleted the same scripts + moved a config the refactor deleted). Pulled main, rebased: commit collapsed to a `.gitignore`-only change → amended message → **pushed 37b4edb** (adds `mustafar/scripts/**/config.json` to gitignore). Working tree == origin/main.
- **config.json (live token `sk-1f83…`, never committed) relocated to `scripts/local/config.json`, still git-ignored** — the refactor removed the file from the repo; keeping it local-only for agentic eval env. Token should still be rotated (also lives as DEFAULT_API_KEY in eval-box `deepswe-main/run_mini_swe.sh`).
- **Deletable in scripts/local/** (no live refs, superseded): `tp4_parse.py`, `collect_262k.py`, `serve_sweep_parse.py`. Borderline (input data): `sangfor-bench-hard50.txt`. Do NOT delete: `bench-serving.sh` (bench-fair/max call it; it is the 3-wave report protocol, distinct from refactor's new `bench_serving.sh` used by Dockerfile/modal/tests), `agentic-eval.sh`, `lb2_serve_eval.py`, `env.sh`, `serve.sh`.

## 09:50 — DeepSWE fully complete (60/60 both legs); report DeepSWE-Bench suite added + pushed (165de50)

- **Corrected earlier 03:47 "native froze at 40/60 / engine dead" report — WRONG.** Fresh aggregation: **both legs 60/60 done**, results on all 60 shared tasks. Spot-check of late-native tasks (koota 04:22 CST, bandit 04:11, go-git-worktree 05:50, actionlint 06:55 finishes) showed real agent work (8.6–12.1M in-tokens, 93–115 steps, claude-code.txt mtime continuous through finish) → a native server served continuously to the end (~08:15 CST); the apparent death was transient or misdiagnosed.
- **Final numbers:** native 35 cap / 25 natural / **5 solved**; packed 36 cap / 24 natural / **7 solved**. 2×2 over 60 shared tasks: both 1 (anko), native-only 4 (clack, go-genai, httpx, kombu), packed-only 6 (abs-module-cache, bandit-interprocedural, cattrs, fd-deterministic, goreleaser, happy-dom), neither 49. Natural-completion pass rates: native 5/25 = 20.0%, packed 7/24 = 29.2%.
- **User reversed the Abandon decision** (premise — partial data — was wrong) → chose "Add DeepSWE now (full data)".
- **report.md updated + pushed (165de50, then b0e0cd3):** Overview para 3 + benchmark intro now mention the third cap-dominated suite; headline table gets `DeepSWE-Bench (n=60, 1 run each) 5 | 7 | +2 tasks`; new `### DeepSWE-Bench` subsection (2×2 matrix + cap-hit explanation: 5400 s agent budget, cap = 0-byte patch/reward 0, ~60% capped; natural-completion pass rates; framed as directional Packed tilt consistent with Sangfor but not noise-tested, weakest of the three suites); Conclusion rewired from "no quality signal beyond noise" → "no consistent quality signal … together read as parity within noise". b0e0cd3 dropped the per-task-name listing from the DeepSWE prose per user request (kept "one shared solve").
- **Both engines confirmed dead; user holds zero GPUs** (0-3 = zyh96034 HiSparse job, port 31100 — untouched; 4-7 free). No live servers on 30211/30212; all DeepSWE results now read-only on the eval box.

## 02:05 — DeepSWE poll: native 24 done (15 cap / 9 natural / 2 solved), packed 25 done (15 cap / 10 natural / 3 solved)

- **Both legs (n=60, c8):** native launched 32/60, packed 33/60; completed 24/25, 8 in flight each, 28/27 not yet started. Last completion 02:00 box-local (finished_at UTC 18:00). Servers :30211/:30212 both healthy (HTTP 200).
- **Reward==1 solved:** native 2 (`clack-async-autocomplete-options`, `httpx-multipart-response-parsing`); packed 3 (`abs-module-cache-flags` new this hour, `cattrs-partial-structuring-recovery`, `goreleaser-retry-publish-auditing`).
- **Cap-hits stay dominant (~60% of completions):** 15 per leg `AgentTimeoutError` @ 5400 s → 0-byte patch, reward 0. Natural completions 9 (native) / 10 (packed).
- **Pacing:** +7 completed per leg in the ~55 min since 01:10 (native 17→24, packed 18→25); launch rate ~8/hr. Rough ETA for last task ≈ 06:00–07:30 box-local (remaining 27-28 to dispatch + ≤90-min cap tail), faster than the earlier 09:00–10:00 guess. `--agent-timeout-multiplier > 1` remains the fix for interpretability on any rerun.

## 00:16 — DeepSWE cap-count poll: 14/22 completed trials hit the 5400s agent timeout

- **Both legs (n=60, c8), DeepSWE-Bench:** 19 trial dirs launched each, 11 completed each. Completed → cap-hit breakdown from `result.json` `exception_info`: native 8/11 (73%) AgentTimeoutError, packed 6/11 (55%). Combined **14/22 = 64%** of completed trials hit the 5400s per-task `[agent] timeout_sec` ceiling.
- **Cap hits are wave-1 concentrated:** all 8 native + 6/8 packed wave-1 trials timed out; the 6 wave-2 trials completed since (~22:55 start, 3/leg) all finished *under* the cap naturally but mostly failed (native solved 0/11, packed solved 1/11 — the one win is wave-1 packed cattrs).
- **Meaning:** a capped trial = 0-byte model.patch + reward 0. Agents were mid-work (running pytest, ~127k in-tokens) when the task-spec 90-min budget fired — not a server stall, not concurrency. Fix for a future launch = `--agent-timeout-multiplier` > 1.
- **Still running:** 8 in flight per leg (19 launched of 60); drains in ~90-min waves. Servers :30211 (native) / :30212 (packed) healthy.
